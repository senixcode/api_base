export enum RouteAuth {
    signup = "/signup",
    signin = "/signin",
}

export enum RouteUser {
    profile = '/profile'
}

export enum NodeEnv {
    prod = 'production',
    dev = 'development',
    test = 'test'
}